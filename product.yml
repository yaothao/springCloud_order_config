spring:
  application:
    name: product
  datasource:
      driver-class-name: com.mysql.cj.jdbc.Driver
      username: root
      password: Shield1601@
      url: jdbc:mysql://127.0.0.1/springCloud_Sell?characterEncoding=utf-8&useSS=false
  jpa:
    show-sql: true
  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
